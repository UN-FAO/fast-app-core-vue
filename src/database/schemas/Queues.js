const queueSchema = {
  'title': 'Queue',
  'description': 'Handles all the queue for actions',
  'version': 0,
  'type': 'object',
  'properties': {
    'element': {
      'type': 'object'
    }
  }
}

export default queueSchema
